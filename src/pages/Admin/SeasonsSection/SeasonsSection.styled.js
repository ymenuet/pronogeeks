import styled from 'styled-components';

import { Form } from '../Admin.styled';

export const SeasonForm = styled(Form)`
  display: flex;
  flex-direction: column;
  justify-content: space-evenly;
  align-items: center;
`;
