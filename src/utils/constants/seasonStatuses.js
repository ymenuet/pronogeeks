const seasonStatuses = {
  UPCOMING: 'UPCOMING',
  UNDERWAY: 'UNDERWAY',
  ENDED: 'ENDED',
};

export const LIST_OF_ALL_SEASON_STATUSES = Object.values(seasonStatuses);

export default seasonStatuses;
